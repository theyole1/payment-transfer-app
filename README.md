Install dependencies
npm install

Run the app with backend

This project uses concurrently to run both the Angular frontend and the JSON server backend at the same time:

npm run start:all

npm:start → Starts the Angular frontend

npm:server → Starts the mock backend server

Access the app

http://localhost:4200

